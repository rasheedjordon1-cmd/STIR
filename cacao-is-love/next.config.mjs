/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // Swap in your CDN / Shopify image domains when real photography lands.
    remotePatterns: [],
  },
}

export default nextConfig
